<h1><%= "#{@event.name}" %> Details</h1>

<%= link_to "Show all events", events_path %>
|
<%= link_to "Edit", edit_event_path %>
|
<%= link_to "Delete", event_path(@event), method: :delete, data: {confirm: "Are you sure you would like to delete #{@event.name}? This cannot be undone."}%>

  <%= render 'single_event', event: @event %>
